{
  "name": "ruter-departures",
  "version": "1.0.0",
  "description": "Fetch departures for a given station",
  "main": "lib/index.js",
  "scripts": {
    "lint": "eslint src",
    "compile": "babel --presets es2015 -d lib/ src/",
    "compilew": "babel --presets es2015 -w -d lib/ src/",
    "fetch": "node lib/index.js"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-preset-es2015": "^6.22.0",
    "eslint": "^3.15.0"
  },
  "dependencies": {
    "lodash": "^4.17.4",
    "moment": "^2.17.1",
    "ruter-api": "^1.0.4"
  }
}
